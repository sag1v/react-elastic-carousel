---
name: onUserNext
order: 999
route: /onUserNext
menu: Examples
---

import {Playground, PropsTable } from 'docz';
import Carousel from 'react-elastic-carousel';
import Item from '../../components/ItemContainer';
import items from '../../components/itemsCollection';

<Playground >
    <Carousel transitionMs={2500} itemsToShow={2} onUserNext={(d) => console.log("onUserNext",d)} onNext={(d) => console.log('onNext',d)}>
        {items(12).map(item => <Item key={item.id} {...item}/>)}
    </Carousel>
</Playground>