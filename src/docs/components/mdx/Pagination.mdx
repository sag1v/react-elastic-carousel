---
name: Pagination
---

import { Playground } from 'docz';
import Carousel from '../../../react-elastic-carousel';
import Item from '../ItemContainer';
import items from '../itemsCollection';

<Playground>
  <Carousel pagination={true}>
    {items(12).map(item => <Item key={item.id} {...item}/>)}
  </Carousel>
</Playground>