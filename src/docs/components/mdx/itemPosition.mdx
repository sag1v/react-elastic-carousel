---
name: itemPosition
order: 9
route: /itemPosition
menu: Examples
---

import {Playground, PropsTable } from 'docz';
import Carousel, {consts} from 'react-elastic-carousel';
import Item from '../../components/SimpleItem';
import items from '../../components/itemsCollection';

- You should import the `consts` object to pass the desired `itemPosition`

```
import Carousel, { consts } from 'react-elastic-carousel';
```

<Playground >
    <Carousel itemPosition={consts.START} itemsToShow={2}>
        <Item maxWidth="50%">1</Item>
        <Item maxWidth="50%">3</Item>
        <Item maxWidth="50%">4</Item>
        <Item maxWidth="50%">5</Item>
    </Carousel>
</Playground>